<Page
    x:Class="JNHub.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JNHub"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="using:JNHub.Utils"
    mc:Ignorable="d">

    <Page.Resources>

        <SolidColorBrush x:Key="YellowColor">#FEC92F</SolidColorBrush>
        <Style x:Key="AboutTextButton" TargetType="Button">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontWeight" Value="SemiLight" />
        </Style>
        <Style x:Key="AboutTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontWeight" Value="SemiLight" />
        </Style>
        <Style x:Key="YellowTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource YellowColor}" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <DataTemplate x:Key="SectionHeadlineItemTemplate">
            <Grid Width="600" Height="320">
                <Grid>
                    <Grid.Background>
                        <ImageBrush ImageSource="{Binding Image}" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <Grid Background="#DE000000" Height="88" VerticalAlignment="Bottom">
                        <Grid Margin="5,5,5,5" VerticalAlignment="Top">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding JNItem.Title}" TextTrimming="CharacterEllipsis" FontSize="22" />
                            <TextBlock Grid.Row="1" Text="{Binding JNItem.FormattedSmallDescription}" TextWrapping="Wrap" TextTrimming="WordEllipsis" Style="{StaticResource YellowTextBlock}"/>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="SectionItemTemplate">
            <Grid Width="300" Height="160">
                <Grid>
                    <Grid.Background>
                        <ImageBrush ImageSource="{Binding Image}" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <Grid Background="#DE000000" Height="60" VerticalAlignment="Bottom">
                        <Grid Margin="5,5,5,5" VerticalAlignment="Top">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding JNItem.Title}" TextTrimming="CharacterEllipsis" FontSize="14" />
                            <TextBlock Grid.Row="1" Text="{Binding JNItem.FormattedPubDate}" TextTrimming="CharacterEllipsis" Style="{StaticResource YellowTextBlock}"/>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </DataTemplate>
        <utils:SectionItemTemplateSelector x:Key="sectionItemDataTemplateSelector" SectionHeadlineItemTemplate="{StaticResource SectionHeadlineItemTemplate}" SectionItemTemplate="{StaticResource SectionItemTemplate}" />
        <DataTemplate x:Key="BasicSectionTemplate">
            <Grid Width="Auto">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="40" />
                    </Grid.RowDefinitions>
                <utils:VariableSizeGridView x:ConnectionId='1' SelectionMode="None" IsItemClickEnabled="True" ItemTemplateSelector="{StaticResource sectionItemDataTemplateSelector}" ItemsSource="{Binding Items}"                                            >
                    <utils:VariableSizeGridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VariableSizedWrapGrid Margin="0,0,0,0" 
                                                   ItemHeight="156" 
                                                   ItemWidth="292" 
                                                   Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </utils:VariableSizeGridView.ItemsPanel>
                </utils:VariableSizeGridView>
                    <Grid Grid.Row="1">
                        <Button x:ConnectionId='2' Content=". . ." VerticalAlignment="Top" HorizontalAlignment="Right" BorderThickness="0" Tag="{Binding}" FontWeight="Bold" FontSize="22"                      />
                    </Grid>
                </Grid>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Name="rootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Hub x:ConnectionId='3'                                            >
            <Hub.HeaderTemplate>
                <DataTemplate>
                    <!-- Back button and page title -->
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <AppBarButton x:Name="backButton" Visibility="Collapsed" Icon="Back" Margin="10,-14,0,0" />
                        <Image VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,-30,0,0" Source="Assets/jovemnerd.png" Height="80" Grid.Column="1" IsHitTestVisible="false" />
                    </Grid>
                </DataTemplate>
            </Hub.HeaderTemplate>
            
            <HubSection Name="headHubSection" Width="800">
                <HubSection.Background>
                    <ImageBrush ImageSource="Assets/banner.jpg" Stretch="UniformToFill" Opacity="0.6" />
                </HubSection.Background>
            </HubSection>
                
            <HubSection x:Name="NerdcastSection" Width="960" Header="{Binding CategoryName}" IsHeaderInteractive="True" ContentTemplate="{StaticResource  BasicSectionTemplate}" >


            </HubSection>

            <HubSection x:Name="NerdOfficeSection" Width="960" Background="Black" Header="{Binding CategoryName}" IsHeaderInteractive="True" ContentTemplate="{StaticResource  BasicSectionTemplate}" >

            </HubSection>

            <HubSection x:Name="NerdPlayerSection" Width="960" Header="{Binding CategoryName}" IsHeaderInteractive="True" ContentTemplate="{StaticResource  BasicSectionTemplate}" >


            </HubSection>

            <HubSection x:Name="MRGSection" Width="960" Background="Black" Header="{Binding CategoryName}" IsHeaderInteractive="True" ContentTemplate="{StaticResource  BasicSectionTemplate}" >


            </HubSection>

            <HubSection x:Name="MRGShowSection" Width="960" Header="{Binding CategoryName}" IsHeaderInteractive="True" ContentTemplate="{StaticResource  BasicSectionTemplate}" >

            </HubSection>
            <HubSection x:Name="AboutSection" Width="300" IsHeaderInteractive="False" >
                <HubSection.Background>
                    <ImageBrush ImageSource="Assets/about_bg.png" Stretch="UniformToFill"  Opacity="0.7" />
                </HubSection.Background>
                <DataTemplate>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical">
                        <Image Source="Assets/adheus_logo.png" Margin="10,0,10,0" />
                        <TextBlock Text="versão 1.0" TextAlignment="Center" Margin="0,20,0,20" Style="{StaticResource AboutTextBlock}"/>
                        <Button x:ConnectionId='4' Content="termos de uso" HorizontalAlignment="Center" Style="{StaticResource AboutTextButton}"                        />
                        <Button x:ConnectionId='5' Content="www.adheus.com" Margin="0,40,0,40" HorizontalAlignment="Center" Style="{StaticResource AboutTextButton}"                  />
                    </StackPanel>
                </DataTemplate>
            </HubSection>
        </Hub>
    </Grid>
</Page>

